const INITIAL_STATE = {
  profile: null,
  profiles: null,
  loading: false
};

const profileReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
